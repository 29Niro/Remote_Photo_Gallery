import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  
  static const Color primary = Colors.purple;
  static const Color secondary = Colors.green;
  static const Color background = Color.fromARGB(255, 200, 200, 200);
  static const Color text = Colors.black;
  static const Color accent = Colors.red;
  static const Color textButtonColor = Colors.white;
}
